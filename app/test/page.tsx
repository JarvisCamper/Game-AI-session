import { GameScene } from "@/feature/game/GameScene";

/**
 * /test — sandbox for the turn-based card battle game.
 * Pick attack or defend on your turn; special is on hold. Use the dev
 * viewpoint switcher to see how each player perceives the same battle.
 */
export default function TestPage() {
  return <GameScene />;
}
